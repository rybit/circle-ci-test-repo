machine:
    ruby:
      version:
        2.3.0

deployment:
  production:
    branch: master
    commands:
      - npm install netlify-cli -g
      - netlify deploy -t $NETLIFY_TOKEN

dependencies:
  override:
    - echo "overries"

test:
  override:
    - echo "test"

